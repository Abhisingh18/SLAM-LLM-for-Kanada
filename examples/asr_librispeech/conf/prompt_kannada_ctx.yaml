dataset_config:
    # we put prompt here, because the hydra override in shell script only support a small subset of chars
    prompt: "Transcribe speech to {lang} text.{lang_note} If available, use this previous context: {prev_context}"
